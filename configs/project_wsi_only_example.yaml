project:
  name: vulvar

analysis:
  run_fusion: false
  clinical_path: null
  clinical_features: []
  outcome_time_col: null
  outcome_event_col: null

paths:
  # Update these to your cohort.
  project_dir: data/example_project
  wsi_dir: data/example_project/wsi
  cache_dir: .cache/image_cache

  # Minimal STAMP slide/label table (required by STAMP preprocess/crossval).
  # Should include at least patient/filename/label columns used by your experiment.
  stamp_table: data/example_project/stamp_table.csv

columns:
  pred_id: patient
  label: recur

outputs:
  preprocess_base: stamp_preprocess
  crossval_base: stamp_crossval
  analysis_output: stamp_analysis

stamp:
  device: cuda
  max_workers: 16

crossval:
  task: classification
  ground_truth_label: recur
  patient_label: patient
  filename_label: filename
  n_splits: 5
